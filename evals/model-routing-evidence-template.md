# Think With Me — model-routing runtime evidence

Use one copy of this template for each fresh-context runtime sample. Preserve
the raw response even when the case fails; do not replace it with a normalized
thread summary.

## Candidate

- Date and host:
- Case ID:
- `CANDIDATE_ID`:
- `PACKAGE_SHA256`:
- Candidate source loaded explicitly:
- Source configuration (`family + effort` running the sample):

## Prompt

```text
Paste the exact prompt here.
```

## Raw `agentMessage.text`

```text
Paste the complete raw agentMessage.text here, including Markdown markers.
```

## Routing invariants

| Criterion | Pass / fail | Evidence or observed deviation |
| --- | --- | --- |
| Required quality contract was identified before cost | | |
| Family and effort were evaluated as one atomic configuration | | |
| Open contextual judgment preserved the `Sol High` floor | | |
| A lower-cost route, if selected, was bounded, verifiable, and materially affected by cost, latency, or volume | | |
| Conflicting or insufficient evidence did not become an equivalence claim | | |
| Exactly one eligible configuration was recommended | | |

## Output invariants

| Criterion | Pass / fail | Evidence or observed deviation |
| --- | --- | --- |
| Useful analysis appears before the closing | | |
| One continuous Markdown blockquote contains all three fields | | |
| The view is conclusive and contains no question | | |
| The next step is one immediate dependency | | |
| A user decision contains the recommended answer and at most one question | | |
| The footer contains one documented family and effort plus one contextual reason | | |
| The footer contains no alternative, transition, or unsupported comparison | | |
| No action was taken without explicit authorization | | |

## Cost evidence

- Host-reported token use:
- Host-reported latency:
- Host-reported agent steps:
- Host-reported total task cost:
- Availability note: write `not exposed by host` for every unavailable value;
  never infer cost from list price alone.

## Observed deviation

- Deviation:
- Likely boundary implicated:
- Follow-up required:

## Result

- Result: `pass` / `fail`
- Reviewer:
- Review date:

