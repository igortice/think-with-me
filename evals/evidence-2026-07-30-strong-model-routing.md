# Think With Me — evidence for the strong-model routing policy

Date: 2026-07-30

RUNTIME_SOURCE_FIDELITY: host-unverified
LOCAL_CANDIDATE_STATUS: passed
INDEPENDENT_REVIEW_STATUS: passed
GLOBAL_PARITY_STATUS: not-run
POST_INSTALL_RUNTIME_STATUS: not-run

No commit, push, publication, global synchronization, or skills.sh installation preceded this candidate evidence record.

## Candidate identity

- `PACKAGE_SHA256`: `ced936a55a1c69db1632224cedba0dabe097131c4434095ff6e06f5bef02285b`
- `INITIAL_RUNTIME_CAPTURES_SHA256`: `2f4f9f028ea64a4379f0b503a30863a4d27206f67f97ad4aeb92200c61a4cdd0`
- `FINAL_RUNTIME_CAPTURES_SHA256`: `9d2e5d803ffdd477df1cc7b8fcbcc0580c33a3e9f64afccad802f4793adfd63b`

The installable package contains exactly one current normative routing policy
and an immutable historical evidence snapshot. The historical snapshot hash is
`13c76308c5a69163c24ccf32443a2ca854073d0fa43c2f6caa0e229d44f5d9e8`.

## Static validation passed

The candidate passed:

- the six-pair semantic gate in
  `tests/strong-model-routing-policy.sh`;
- shell syntax checks for the validators;
- synchronized model-comparison mirror checks;
- preserved DeepSWE source-data verification;
- `git diff --check`.

The semantic gate checks the exact allowlist, mutually exclusive routing rows,
host-control boundary, examples, evaluations, research matrix, current-policy
evidence, and immutable historical snapshot.

## Runtime behavior passed

The baseline captures exposed stale host-loaded behavior for project discovery,
structured volume, and agent execution. Those outputs are negative
source-fidelity evidence and are preserved in
`runtime-captures-2026-07-30-strong-model-routing.md`.

The final independent evaluators read the exact local `SKILL.md`,
`model-routing.md`, and `output-contract.md` paths before responding. The final
capture records successful routes for:

1. `Sol Medium` — project conversation and architecture discovery;
2. `Sol High` — architecture trade-off and professional judgment;
3. `Sol XHigh` — residual contradiction and difficult-to-detect risk;
4. `Sol Max` — final critical maximum-depth gate;
5. `Terra Max` — heterogeneous agents, tools, and heavy multi-step execution;
6. `Luna Max` — homogeneous high-volume work with fixed schema and automatic
   validation.

The host-unavailability scenario also passed: the response preserved the
recommendation, reported the mismatch, required an explicit host-level choice,
and did not claim selection, retry, blocking, replacement, or verification of
the host model.

## Independent review

Each final scenario ran in an isolated evaluator prohibited from reading evals
or tests and prohibited from editing files. The agent identifiers and observed
closings are bound in
`runtime-captures-2026-07-30-strong-model-routing-final.md`.

The review distinguishes candidate behavior from host source fidelity. A
correct response after explicit local-path loading proves the candidate
contract; a stale response after name-based loading proves only that global
parity must be checked separately.

## Global parity

Not run before this local candidate evidence record. Global synchronization
must occur only after this record verifies against the current package hash.

## Post-install runtime

Not run before this local candidate evidence record. After synchronization, a
fresh name-based invocation must prove that the host no longer reproduces the
stale routes.
