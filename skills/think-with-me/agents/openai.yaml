interface:
  display_name: "Think With Me"
  short_description: "Think through choices and select the next GPT-5.6 model"
  default_prompt: "Use $think-with-me to understand this conversation, give your view, recommend the immediate next step, and select the GPT-5.6 model for that step in my language."

policy:
  allow_implicit_invocation: true
